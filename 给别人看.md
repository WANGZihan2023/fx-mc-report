# 对方没装 Python 时

**结论：只能发浏览器链接。** 不能发双击脚本（脚本仍要 Python）。

---

## 现在就能发（你电脑开着）

你本机跑着报告时，开一个「公网隧道」，对方用手机/电脑浏览器打开即可。

在项目文件夹执行：

```bash
./分享链接.sh
```

会打印一个 `https://....loca.lt` 或类似地址，把该地址发给对方。  
**注意：你关电脑或关终端，链接就失效。**

---

## 长期可发（推荐）：Streamlit 云网址

对方永远不用装任何东西，链接长期有效。

### 你需要做一次（约 5 分钟）

1. 终端登录 GitHub（你当前 token 已失效）：
   ```bash
   gh auth refresh -h github.com
   ```
2. 在本项目提交并创建仓库：
   ```bash
   cd "/Users/wangzihan/Desktop/工作_汇率/usd_aud_mc_report"
   ./发布到云端.sh
   ```
3. 打开 https://share.streamlit.io/ → 用 GitHub 登录 → **New app**  
   - Repository：刚创建的仓库  
   - Main file path：`app.py`  
   - Deploy  
4. 得到 `https://xxxxx.streamlit.app` → **只发这个链接给对方**

---

## 不要用的方式

| 方式 | 原因 |
|------|------|
| 一键打开.bat / .command | 对方没 Python 会失败 |
| 只发文件夹 | 对方打不开 |
| 打包 exe（费事且 Streamlit 不稳） | 不推荐 |

---

**一句话：没 Python 的人 = 只给他们一个 https 链接。**
