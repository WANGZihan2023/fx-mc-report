# 复制为仓库根目录 .env（已 gitignore）。
# 本机 Streamlit：API 配置页「保存到本机 .env」会写入本文件 + vault。
# Railway 公网站：
#   - 「关掉再开还在」→ Railway Variables（一次）；或 ./scripts/push_env_to_railway.sh
#   - 「下载 .env」存本机后，下次用网页「从本机 .env 上传」恢复本会话
#   - 页面「保存」只写服务器临时盘，不会到你的 Mac
# 共享 vault：/Users/wangzihan/Desktop/工作_汇率/fx_data_apis/.env
# 勿提交真实 Key。

# --- 推荐（决定 References / 证据条数）---
FRED_API_KEY=
NEWSAPI_KEY=
FINNHUB_API_KEY=

# --- 可选行情增强 ---
ALPHA_VANTAGE_API_KEY=
TWELVE_DATA_API_KEY=

# --- 可选 AI 检索（给检索员更多原料；LLM 本身不抓新闻）---
TAVILY_API_KEY=
BRAVE_SEARCH_API_KEY=

# --- LLM（判定/抽取；不会凭空增加 References URL）---
# Ollama：Key=ollama，Base=http://127.0.0.1:11434/v1
# Groq：填 GROQ_API_KEY，或 LLM_* 指向 api.groq.com
# DeepSeek：填 DEEPSEEK_API_KEY（或 LLM_API_KEY）+ LLM_BASE_URL=https://api.deepseek.com/v1
#           模型 deepseek-chat；勿把 DeepSeek Key 配到 api.openai.com
GROQ_API_KEY=
DEEPSEEK_API_KEY=
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
