# Streamlit / 本地大模型配置示例
# 复制为 secrets.toml（Streamlit）或导出为环境变量

# OpenAI 官方：
# OPENAI_API_KEY = "sk-..."
# LLM_MODEL = "gpt-4o-mini"

# DeepSeek 示例：
# LLM_API_KEY = "sk-..."
# LLM_BASE_URL = "https://api.deepseek.com/v1"
# LLM_MODEL = "deepseek-chat"

# 也可在侧栏临时粘贴 API Key（仅当前会话，不写磁盘）
